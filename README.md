# Inverted-Right-Angled-Number-Triangle
The reverse of the increasing number triangle pattern. Outer loop → controls rows (starting from 5 down to 1).  Inner loop → prints numbers from 1 up to the current row number i.
